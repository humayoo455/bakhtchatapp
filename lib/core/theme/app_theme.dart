import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF0D0204); // Deep black-maroon
  static const Color surface = Color(0xFF1A0A0E);    // Dark card color
  static const Color accent = Color(0xFFE91E63);     // Hot pink/maroon accent
  static const Color highlight = Color(0xFF3D1219);  // Subtle maroon glow
}
